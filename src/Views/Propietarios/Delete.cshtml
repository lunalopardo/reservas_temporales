@model ReservasTemporales.Models.Propietario

@{
    ViewData["Title"] = "Eliminar propietario";
}

<link rel="stylesheet" href="~/css/formularios.css" />
<link rel="stylesheet" href="~/css/site.css" />

<div class="formulario-contenedor">
    <h2 class="titulo-vista">Eliminar propietario</h2>

    <p class="subtitulo-alerta">¿Estás seguro de que querés dar de baja a este propietario?</p>

    <div class="detalle-datos">
        <p><strong>Código:</strong> @Model.IdPropietario</p>
        <p><strong>Nombre:</strong> @Model.Nombre @Model.Apellido</p>
        <p><strong>DNI:</strong> @Model.Dni</p>
        <p><strong>Teléfono:</strong> @Model.Telefono</p>
        <p><strong>Email:</strong> @Model.Email</p>
    </div>

    <form asp-action="Delete" method="post">
        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="IdPropietario" />

        <div class="acciones">
            <button type="submit" class="boton-eliminar">Confirmar baja</button>
            <a asp-action="Index" class="boton-volver">Cancelar</a>
        </div>
    </form>
</div>